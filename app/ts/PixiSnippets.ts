/*
    var brt = new PIXI.BaseRenderTexture(300, 200, PIXI.SCALE_MODES.LINEAR, 1);
    var rt = new PIXI.RenderTexture(brt);
    var sp = new PIXI.Sprite(rt);

    

    var renderTexture = PIXI.RenderTexture.create(100, 100);
    renderer.render(mySprite, renderTexture);


    var graphics = new PIXI.Graphics();
    graphics.beginFill(0xFFFF00);
    graphics.lineStyle(5, 0xFF0000);
    graphics.drawRect(0, 0, 300, 200);

    whitesleeve.position.y = 100;
    whitesleeve.texture.baseTexture = mesh.texture.baseTexture;
    
    Renderer.getInstance().AddAnySprite(whitesleeve);
    Renderer.getInstance().AddAnySprite(graphics);


          // this.dragonFactory.replaceSlotDisplay(
      //   "template_character",                 // dragonBonesName
      //   "herocharacter",                      // armatureName
      //   meshName,                             // slotName
      //   copyslot.name,                        // newDisplayName
      //   _bone.armature.getSlot(meshName)      // slot
      // );

*/